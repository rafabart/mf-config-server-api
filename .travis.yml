language: java
jdk:
  - oraclejdk11
script:
  - ./gradlew clean test
  - ./gradlew build

before_install:
  - chmod +x gradlew
  - export TZ=Brazil/East

deploy:
  provider: heroku
  app: mf-config-server-api
  api_key: $HEROKU_API_KEY
